{
  "name": "workadventure-map-starter-kit",
  "version": "3.3.11",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "tsc && vite build && node scripts/fix-wa-script-wrappers.mjs",
    "prod": "vite preview",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "server:dist": "npm run serve:dist",
    "serve:dist": "npx --yes serve dist -l 4173 -C --no-etag --no-port-switching",
    "tunnel:dist": "npx --yes localtunnel --port 4173",
    "tunnel:dist:keepalive": "while true; do npm run tunnel:dist; echo '[tunnel] disconnected, retry in 2s'; sleep 2; done",
    "start:dist": "npm run build && concurrently -k -n serve,tunnel -c blue,magenta \"npm run serve:dist\" \"npm run tunnel:dist\"",
    "start:live": "NODE_OPTIONS='' concurrently -k -n dev,tunnel -c cyan,magenta \"VITE_OPEN=false NODE_OPTIONS='' vite --host 0.0.0.0 --port 4173 --strictPort\" \"NODE_OPTIONS='' npm run tunnel:dist:keepalive\""
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@workadventure/iframe-api-typings": "^1.30.1",
    "concurrently": "^9.2.1",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "vite-plugin-node": "^7.0.0",
    "wa-map-optimizer-vite": "^1.3.1"
  },
  "dependencies": {
    "@types/suncalc": "^1.9.2",
    "@workadventure/scripting-api-extra": "^1.9.12",
    "cron-schedule": "^5.0.4",
    "suncalc": "^1.x.x"
  }
}
